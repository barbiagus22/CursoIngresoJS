function mostrar()
{
	console.log('iteración while');

	var contador;
	contador=0;

	while (contador<10) {

		contador= contador + 1;
		alert('mensaje' + contador);
		
		
		
	}


	//while hace q la condicion se evalue una y otra vez.
	// tengo que asegurar que cuando ingrese el dato salga, para eso contador = 1
	// a dif de if se sigue ejecutando

	// es contador por que va contando sobre si mismo

	// contador+1 es lo mismo que contador++ le suma uno al contador.
	 // en el con .log se pone la cant de veces que se quiere ejecutar


	 /* 1ro iniciarlo 
	 2do la cant de veces que qiero que se repita
	 3ro no olvidarme de que vuelva a ejecutar contador+1



	 */
}//FIN DE LA FUNCIÓN