function mostrar()
{
//tomo la edad  

var edad;

edad=document.getElementById('edad').value;
edad=parseInt(edad);

if  (edad==15)
{

alert("niña bonita");


 }

}

/*
Sentencia IF ninguna lleva punta y coma. 
se ingresa la condicion y se evalue como true o false.
se compara un valor con otro valor.

OPERADORES BINARIOS. 
== operador de igual si lo que esta a la izq  y derecha son iguales. 
ej edad== 18
!= distinto niega algo ej !true va a negar el valor por lo cual es false.
>= mayor o igual
<= menor igual
>
<

=== igual extricta , compara los tiposde valores, los tipos de variables. 

streming "true"
true boolean
binario por que necesita dos operando uno a la izq y otro a la derecha.


OPERADORES LOGICOS:

Mas de una expresion, ej && y el  ||  (queremos evaluar mas de una condicion)

|| sig o EJ: 

If se ejecuta cuando es verdadero.
ej 

else s ejecuta cuando no s ejecuta el if. por cada if va un else. 
se ejecuta cuando es falso el if
if es verdadero.

// El Else se ingresa en entre 


if (edad==15);
alert ();





*/




// /FIN DE LA FUNCIÓN